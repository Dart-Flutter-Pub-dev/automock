name: automock
description: An automatic mock server to speed up your initial development.
version: 1.0.1
homepage: https://github.com/mauriciotogneri/automock

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  json_annotation: ^3.0.1

dev_dependencies:
  json_serializable: ^3.3.0
  build_runner: ^1.8.1
  http: ^0.12.0+4
  test: ^1.14.2